PORT=3001
JWT_SECRET=cambia_esto_por_una_cadena_larga_y_aleatoria_en_produccion
JWT_EXPIRES_IN=8h
DB_PATH=./optimind.db
NODE_ENV=development
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ─── Gmail SMTP ────────────────────────────────────────────────────────────
# 1. Ve a: myaccount.google.com → Seguridad → Verificación en dos pasos (actívala)
# 2. Busca "Contraseñas de aplicaciones" → crea una para "Correo / Windows"
# 3. Copia los 16 caracteres generados en SMTP_PASS
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=abcd efgh ijkl mnop    # 16 chars sin espacios
SMTP_FROM=OptiMind Solutions <tu_correo@gmail.com>
